local M = require("block")
M.setup({
    automatic=false,
})
-- vim.api.nvim_create_autocmd('FileType', {
--     group = 'block.nvim',
--     pattern = '*',
--     callback = function(args)
--         M.on()
--     end
-- })
